# golden-60 レビューシート

ループ設計 §3.3 規律 G1 の評価データセット。**候補の選定は機械的に済んでいる**ので、
残っているのは**期待値の確定**（人手）だけである。

- 候補ファイル: `scripts/retail-stats-tracker/tests/fixtures/golden-60.candidates.jsonl`
- 確定後の凍結先: `scripts/retail-stats-tracker/tests/fixtures/golden-60.jsonl`
- 構成の検査: `scripts/retail-stats-tracker/tests/test_golden60.py`

本シートは**読むための資料**であり、記入先ではない。各行に候補 JSONL の行番号を
添えてあるので、確定したらその行の `expected` を埋め `status` を `confirmed` にする。

> **「決めること」列は質問であって答えではない。** 当初は機械推定の業態・指標を
> 出していたが、それは人手確定へのアンカリングになる（`3月消費支出…外食マイナス―総務省`
> が「外食」の部分一致で `family-restaurant` にヒットする等、実際に誤りも含んでいた）。
> **判断は必ずタイトル原文から行うこと。**

---

## 先に決めてほしい 3 点（**2026-08-08 オーナー決裁済み**）

> **決裁: G-1 / G-2 / G-4 いずれも実装側の推奨どおり採用**（オーナー SAS-Sasao、2026-08-08）。
> あわせて 58 行の期待値を確認し、`golden-60.jsonl` を
> `decided_by = "owner-confirmed (SAS-Sasao)"` で凍結した。
>
> これにより未決事項 **(c) ランキング記事を分母から外すか** が決着した
> （→ 外す）。未決事項 (a) 左窓の緩和 / (b) 定性表現の分子算入は**引き続き未決**。

**凍結時に 6 行の期待値を訂正した。** 候補作成が v0.1.2 より前で、個社並記の行に
7 値時代の `out_of_scope` が入っていた。要件 制約 16 が同型の例を挙げて
「(a) 個社の並記は行全体を `company_disclosure` とする」と定めているため、
**設計の条文に合わせて `company_disclosure` へ**直した。分母から除外される点は
どちらも同じで、NFR-05 の値は動かない。訂正は各行の `expectation_note` に記録した。

以下は決裁時点の推奨内容（記録として残す）。**推奨であって決定ではない**という
但し書きは決裁前のもの。根拠の詳細は `docs/design/origin.md` D-E の N-1a を参照。

| # | 論点 | 実装側の推奨 | 決裁 |
|---|---|---|---|
| **G-2**<br>（最重要） | ランキング記事を NFR-05 の分母から外すか（未決事項 (c)） | **`out_of_scope`（外す）。ただし「数値が改善するから」を理由にしない。** 要件 v0.1.1 の「小売月次統計でない一般記事」に素直に該当するため、定義のまま処理でき要件改訂も不要。**定義の問題として先に決め、数値の動きは事後に報告する**順序を推奨 | **○ 採用** |
| **G-4** | 分母の帰属が未定義のマクロ統計行の扱い | **「日本国内の小売・外食の販売動向を示す統計か」を唯一の線引きに。** 海外マクロ（米/ユーロ圏 GDP・米 CPI）→ `out_of_scope` / 地域別 CPI（都内物価）→ `no_segment_match`（要件 制約 15 が例示）/ 国内の需要側マクロ（消費支出・消費者態度）→ `no_segment_match` / 農林水産物輸出額 → `out_of_scope`。**分母は増える方向**に働き、G-2 と逆向きになる | **○ 採用** |
| **G-1** | 区分①の補充 6 件（全店系）でよいか | **このまま 12 + 6 で凍結。母集団は広げない。** G1 の母集団は「計測日 2026-07-26 の 595 行」と明示されており、窓を広げると設計の他の実測値と比較できなくなる。補充 6 件は業態・期間・発表主体の解決評価には使える。ただし **golden-60 単体では NFR-04 を判定できない**（既存店指標が 12 件しかない）ことを記録する | **○ 採用** |

**この 3 点はいずれも「取れた／取れない」の境界を動かす**ため、個別の 60 行を埋める前に
決裁した。とくに G-2 と G-4 は NFR-05 の分母定義そのものである。

---

## 期待値の書き方

`expected` は次のどちらかの形にする。

**(A) observation が取れる行** — 配列。1 行が複数の observation に分解される場合は複数要素。

```json
"expected": [
  {
    "segment_id": "shopping-center",
    "metric_id": "existing-store-sales-yoy",
    "scope": "existing_store",
    "source_authority": "sc-association",
    "period_key": "2026-06",
    "period_type": "month",
    "value": -1.6,
    "unit": "percent_yoy",
    "streak_broken_months": 51,
    "sign_only": null,
    "needs_source_check": false
  }
]
```

**(B) 解けないことが正解の行** — `unresolved` を持つオブジェクト。

```json
"expected": {"unresolved": {"reason_code": "no_numeric"}}
```

`reason_code` は **9 値**（v0.1.2 で 2 値追加）: `no_metric_match` / `no_segment_match` /
`no_numeric` / `ambiguous_period` / `low_confidence` / `llm_schema_error` / `out_of_scope` /
**`company_disclosure`** / **`no_metric_match_in_multi_value`**。

個社の並記（`ドラッグストア／2月既存店売上ツルハ4.0%増、コスモス薬品7.0%増`）は
**`company_disclosure`**。業態が解決しない行の `out_of_scope` とは検出位置が違う
（要件 制約 16）。分母から外れる点は同じ。

### 記入例（**設計からの転記**であって推奨値ではない）

下の 2 例は実装設計 §7.2 の T-10h / T-10i が期待値を本文で明示しているものを、
そのまま JSON にしたもの。**書式の見本**として使ってほしい。

`ショッピングセンター／6月既存店売上1.6％減、夏物振わず51カ月ぶりに前年割れ`
（T-10h: `existing-store-sales-yoy = -1.6` かつ `streak_broken_months = 51`）

```json
"expected": [
  {
    "segment_id": "shopping-center", "metric_id": "existing-store-sales-yoy",
    "scope": "existing_store", "source_authority": "sc-association",
    "period_key": "2026-06", "period_type": "month",
    "value": -1.6, "unit": "percent_yoy",
    "streak_broken_months": 51, "sign_only": null, "needs_source_check": false
  }
]
```

定性表現のみの行（T-10i: `増収増益` は 2 件、`value = None`、`sign_only = "+"`、
`needs_source_check = True`）

```json
"expected": [
  {"segment_id": "...", "metric_id": "operating-revenue-yoy", "scope": "n_a",
   "source_authority": "...", "period_key": "...", "period_type": "...",
   "value": null, "unit": "percent_yoy",
   "streak_broken_months": null, "sign_only": "+", "needs_source_check": true},
  {"segment_id": "...", "metric_id": "operating-profit-yoy", "scope": "n_a",
   "source_authority": "...", "period_key": "...", "period_type": "...",
   "value": null, "unit": "percent_yoy",
   "streak_broken_months": null, "sign_only": "+", "needs_source_check": true}
]
```

### 埋めるときの原則（G1 の趣旨）

- **記事タイトルに現れない情報を推測で埋めない。** タイトルから読み取れないものは
  そもそも取れないのが正解であり、その場合は (B) を選ぶ
- **「取れないことが正解」「2 レコードが共存するのが正解」を正しく書く。** ここを
  「取れる」側に倒すと、評価が「取れた数」だけを報酬にしてしまい、無理に数値を
  ひねり出す方向・母集団の違う値を 1 つに畳む方向へ最適化が進む（G1 本文）
- 迷った行は `status` を `needs_human_review` のまま残してよい。**未確定を
  `confirmed` と書かないこと**が、この評価データの価値を守る

---


## 作業量の目安

- **(A) observation を書く行: 48 件** （うち複数レコードに分解しうる行 18 件、「既存店」表記あり 18 件）
- **(B) `reason_code` を選ぶだけの行: 10 件**

(B) は 1 行あたり選択肢 9 値から 1 つ選ぶだけなので速い。時間がかかるのは (A) のうち複数指標に分解する 18 件で、ここが FR-11 の評価そのものになる。

---

## ① 主要 4 業態の月次既存店指標（18 件）

NFR-04 が 90% を要求している対象そのもの。**うち 6 件は「既存店」表記が無い全店系**（厳密条件に一致する一意 URL が 12 件しか実在しないための補充枠。判断 G-1）。補充枠は「主要4業態の月次統計 かつ 値あり」に限定してあり個社決算は混ざっていない。`selected_because` で区別できる。

| 行 | 掲載日 | 記事タイトル | **決めること** |
|---:|---|---|---|
| 13 | 2026-04-21 (+3) | コンビニエンスストア／3月既存店2.2％増、行楽需要で揚げ物・ドリンク好調 | `scope` = `existing_store`（「既存店」表記あり）でよいか<br>`period_key`: タイトルに年が無ければ**掲載月の前月**（カタログ §4.2） |
| 14 | 2026-04-25 (+2) | ショッピングセンター／3月既存店売上4.0％増、春物衣料・新生活関連品などが好調 | `scope` = `existing_store`（「既存店」表記あり）でよいか<br>`period_key`: タイトルに年が無ければ**掲載月の前月**（カタログ §4.2） |
| 15 | 2026-04-29 | 日本チェーンストア協会／3月既存店1.7%減 | `scope` = `existing_store`（「既存店」表記あり）でよいか<br>`period_key`: タイトルに年が無ければ**掲載月の前月**（カタログ §4.2） |
| 16 | 2026-05-21 (+1) | コンビニエンスストア／4月既存店0.01%増、セット割引などで揚げ物・冷凍食品が好調 | `scope` = `existing_store`（「既存店」表記あり）でよいか<br>`period_key`: タイトルに年が無ければ**掲載月の前月**（カタログ §4.2） |
| 17 | 2026-05-30 | 百貨店／4月の販売額4.5％増の4840億円、既存店は5.6％増（経産省調べ） | **何レコードに分解するか**（% トークン 2 個。FR-11）<br>`scope` = `existing_store`（「既存店」表記あり）でよいか<br>`period_key`: タイトルに年が無ければ**掲載月の前月**（カタログ §4.2） |
| 18 | 2026-06-25 | 日本チェーンストア協会／5月既存店2.9％増 | `scope` = `existing_store`（「既存店」表記あり）でよいか<br>`period_key`: タイトルに年が無ければ**掲載月の前月**（カタログ §4.2） |
| 19 | 2026-06-26 | ショッピングセンター／5月既存店売上9.9％増、前年より休日数増・イベント施策奏功 | `scope` = `existing_store`（「既存店」表記あり）でよいか<br>`period_key`: タイトルに年が無ければ**掲載月の前月**（カタログ §4.2） |
| 20 | 2026-07-23 (+2) | コンビニエンスストア／6月既存店0.1％減、販促策が奏功し弁当・揚げ物は好調 | `scope` = `existing_store`（「既存店」表記あり）でよいか<br>`period_key`: タイトルに年が無ければ**掲載月の前月**（カタログ §4.2） |
| 21 | 2026-07-23 (+2) | 日本チェーンストア協会／6月既存店1.7％減、天候不順で食品・衣料品ともに苦戦 | `scope` = `existing_store`（「既存店」表記あり）でよいか<br>`period_key`: タイトルに年が無ければ**掲載月の前月**（カタログ §4.2） |
| 22 | 2026-03-31 | 日本百貨店協会／2月の売上高は1.6%増、国内顧客売上が好調で2カ月連続でプラス | **「既存店」表記なし** → 既存店指標に**昇格させない**。率なら `all-store-sales-yoy` / 絶対額なら `sales-amount-absolute`（カタログ §2.2）<br>`period_key`: タイトルに年が無ければ**掲載月の前月**（カタログ §4.2） |
| 23 | 2026-04-04 | 大手百貨店／3月売上高 三越伊勢丹7.6%増、H2O6.5%増 | **何レコードに分解するか**（% トークン 2 個。FR-11）<br>**「既存店」表記なし** → 既存店指標に**昇格させない**。率なら `all-store-sales-yoy` / 絶対額なら `sales-amount-absolute`（カタログ §2.2）<br>`period_key`: タイトルに年が無ければ**掲載月の前月**（カタログ §4.2） |
| 24 | 2026-04-28 | 日本百貨店協会／3月の外国人売上5.2％増、客数12.3％減・客単価19.9％増 | **何レコードに分解するか**（% トークン 3 個。FR-11）<br>**「既存店」表記なし** → 既存店指標に**昇格させない**。率なら `all-store-sales-yoy` / 絶対額なら `sales-amount-absolute`（カタログ §2.2）<br>`period_key`: タイトルに年が無ければ**掲載月の前月**（カタログ §4.2） |
| 25 | 2026-06-26 | 日本百貨店協会／5月の外国人売上16.7％増、客数6.3％減・客単価24.5％増 | **何レコードに分解するか**（% トークン 3 個。FR-11）<br>**「既存店」表記なし** → 既存店指標に**昇格させない**。率なら `all-store-sales-yoy` / 絶対額なら `sales-amount-absolute`（カタログ §2.2）<br>`period_key`: タイトルに年が無ければ**掲載月の前月**（カタログ §4.2） |
| 26 | 2026-07-25 (+1) | 日本百貨店協会／6月の売上高2.3％増、インバウンド29.8％増・国内顧客0.2％減 | **何レコードに分解するか**（% トークン 3 個。FR-11）<br>**「既存店」表記なし** → 既存店指標に**昇格させない**。率なら `all-store-sales-yoy` / 絶対額なら `sales-amount-absolute`（カタログ §2.2）<br>`period_key`: タイトルに年が無ければ**掲載月の前月**（カタログ §4.2） |
| 27 | 2026-07-25 (+1) | 日本百貨店協会／6月の外国人売上29.8％増、客数0.5％減・客単価30.4％増 | **何レコードに分解するか**（% トークン 3 個。FR-11）<br>**「既存店」表記なし** → 既存店指標に**昇格させない**。率なら `all-store-sales-yoy` / 絶対額なら `sales-amount-absolute`（カタログ §2.2）<br>`period_key`: タイトルに年が無ければ**掲載月の前月**（カタログ §4.2） |
| 28 | 2026-03-31 | 日本百貨店協会／2月の外国人売上15.5%減、約453億6000万円に | **「既存店」表記なし** → 既存店指標に**昇格させない**。率なら `all-store-sales-yoy` / 絶対額なら `sales-amount-absolute`（カタログ §2.2）<br>`period_key`: タイトルに年が無ければ**掲載月の前月**（カタログ §4.2） |

選定根拠の内訳:

- (9 件) 主要4業態の別名に一致 かつ 「既存店」を含む（G1 の厳密条件）
- (7 件) 主要4業態の月次統計だが「既存店」表記なし = **全店系**（厳密条件が12 件しか実在しないための補充枠。期待値の scope を要確認）


## ② 複数指標を含む記事（8 件・FR-11）

1 行が複数の observation に分解される。`日本百貨店協会／6月の外国人売上29.8％増、客数0.5％減・客単価30.4％増` = **3 レコード**。

| 行 | 掲載日 | 記事タイトル | **決めること** |
|---:|---|---|---|
| 33 | 2026-03-31 | ドラッグストア／2月既存店売上ツルハ4.0%増、コスモス薬品7.0%増 | **何レコードに分解するか**（% トークン 2 個。FR-11）<br>`scope` = `existing_store`（「既存店」表記あり）でよいか<br>`period_key`: タイトルに年が無ければ**掲載月の前月**（カタログ §4.2） |
| 34 | 2026-04-01 | スーパーマーケット／2月既存店売上ライフ2.4％増、ヤオコー3.3％増 | **何レコードに分解するか**（% トークン 2 個。FR-11）<br>`scope` = `existing_store`（「既存店」表記あり）でよいか<br>`period_key`: タイトルに年が無ければ**掲載月の前月**（カタログ §4.2） |
| 35 | 2026-04-21 (+3) | ファミレス／3月既存店 すかいらーく2.3%増、サイゼリヤ15.5%増 | **何レコードに分解するか**（% トークン 2 個。FR-11）<br>`scope` = `existing_store`（「既存店」表記あり）でよいか<br>`period_key`: タイトルに年が無ければ**掲載月の前月**（カタログ §4.2） |
| 36 | 2026-04-24 (+2) | ドラッグストア／3月既存店売上ツルハ3.3％増、コスモス薬品6.3％増 | **何レコードに分解するか**（% トークン 2 個。FR-11）<br>`scope` = `existing_store`（「既存店」表記あり）でよいか<br>`period_key`: タイトルに年が無ければ**掲載月の前月**（カタログ §4.2） |
| 37 | 2026-04-29 (+1) | スーパーマーケット／3月既存店売上ライフ1.3%増、ヤオコー2.1%増 | **何レコードに分解するか**（% トークン 2 個。FR-11）<br>`scope` = `existing_store`（「既存店」表記あり）でよいか<br>`period_key`: タイトルに年が無ければ**掲載月の前月**（カタログ §4.2） |
| 38 | 2026-05-08 | 家電大型専門店／3月の販売額は4.4％増の5015億円、店舗数0.5％減（経産省調べ） | **何レコードに分解するか**（% トークン 2 個。FR-11）<br>**「既存店」表記なし** → 既存店指標に**昇格させない**。率なら `all-store-sales-yoy` / 絶対額なら `sales-amount-absolute`（カタログ §2.2）<br>`period_key`: タイトルに年が無ければ**掲載月の前月**（カタログ §4.2） |
| 39 | 2026-05-08 | ホームセンター／3月の販売額は3.4％増2868億円、店舗数は0.7％増（経産省調べ） | **何レコードに分解するか**（% トークン 2 個。FR-11）<br>**「既存店」表記なし** → 既存店指標に**昇格させない**。率なら `all-store-sales-yoy` / 絶対額なら `sales-amount-absolute`（カタログ §2.2）<br>`period_key`: タイトルに年が無ければ**掲載月の前月**（カタログ §4.2） |
| 40 | 2026-05-08 | ドラッグストア／3月の販売額5.8％増8325億円、店舗数2.5％増（経産省調べ） | **何レコードに分解するか**（% トークン 2 個。FR-11）<br>**「既存店」表記なし** → 既存店指標に**昇格させない**。率なら `all-store-sales-yoy` / 絶対額なら `sales-amount-absolute`（カタログ §2.2）<br>`period_key`: タイトルに年が無ければ**掲載月の前月**（カタログ §4.2） |


## ③ 期間表記の全 5 種（8 件）

月次 / 決算期 / 四半期 / 半期 / 年度 の 5 種が全て含まれる（制約 5）。

| 行 | 掲載日 | 記事タイトル | **決めること** |
|---:|---|---|---|
| 41 | 2026-04-01 | コメの民間在庫1.5倍に＝2月末、販売が停滞 | **「既存店」表記なし** → 既存店指標に**昇格させない**。率なら `all-store-sales-yoy` / 絶対額なら `sales-amount-absolute`（カタログ §2.2）<br>`period_key`: タイトルに年が無ければ**掲載月の前月**（カタログ §4.2） |
| 42 | 2026-04-13 | イオン、営業益過去最高＝セブンはコスト増重し — 26年2月期 | **「既存店」表記なし** → 既存店指標に**昇格させない**。率なら `all-store-sales-yoy` / 絶対額なら `sales-amount-absolute`（カタログ §2.2）<br>`period_key` / `period_type`（fiscal_period） |
| 43 | 2026-05-16 | 楽天の2026年1-3月期（1Q）の流通総額は約1.5兆円で4.8%増、MNO事業本格参入後初の黒字 | **「既存店」表記なし** → 既存店指標に**昇格させない**。率なら `all-store-sales-yoy` / 絶対額なら `sales-amount-absolute`（カタログ §2.2）<br>`period_key` / `period_type`（quarter） |
| 44 | 2026-04-13 | ファストリ、過去最高益＝ユニクロが国内外で好調 — 中間決算 | **「既存店」表記なし** → 既存店指標に**昇格させない**。率なら `all-store-sales-yoy` / 絶対額なら `sales-amount-absolute`（カタログ §2.2）<br>`period_key` / `period_type`（half） |
| 45 | 2026-04-16 | 楽天2025年度通期決算 国内ECは1兆円突破、モバイルとのシナジー拡大とAI活用推進 | **「既存店」表記なし** → 既存店指標に**昇格させない**。率なら `all-store-sales-yoy` / 絶対額なら `sales-amount-absolute`（カタログ §2.2）<br>`period_key` / `period_type`（fiscal_year） |
| 46 | 2026-05-04 | ヤマト運輸、佐川急便、日本郵便の2026ゴールデンウィーク対応まとめ／アシックスの2025年度EC売上は1484億円で8.3%増。EC化率は18.3% | **何レコードに分解するか**（% トークン 2 個。FR-11）<br>**「既存店」表記なし** → 既存店指標に**昇格させない**。率なら `all-store-sales-yoy` / 絶対額なら `sales-amount-absolute`（カタログ §2.2）<br>`period_key` / `period_type`（fiscal_year） |
| 47 | 2026-06-22 | ニトリHD／中国・上海市に「NITORI 上海PRISMA新嘉中心店」6月26日オープン | **「既存店」表記なし** → 既存店指標に**昇格させない**。率なら `all-store-sales-yoy` / 絶対額なら `sales-amount-absolute`（カタログ §2.2）<br>`period_key`: タイトルに年が無ければ**掲載月の前月**（カタログ §4.2） |
| 48 | 2026-04-06 | 2月農林水産物・食品輸出額 19ヵ月連続増 前年同月比3.3％増 | **「既存店」表記なし** → 既存店指標に**昇格させない**。率なら `all-store-sales-yoy` / 絶対額なら `sales-amount-absolute`（カタログ §2.2）<br>`period_key`: タイトルに年が無ければ**掲載月の前月**（カタログ §4.2） |

選定根拠の内訳:

- (1 件) 期間表記 = fiscal_period（5 種を 1 件ずつ確保する枠）
- (1 件) 期間表記 = fiscal_year（5 種を 1 件ずつ確保する枠）
- (1 件) 期間表記 = half（5 種を 1 件ずつ確保する枠）
- (1 件) 期間表記 = month（5 種を 1 件ずつ確保する枠）
- (1 件) 期間表記 = quarter（5 種を 1 件ずつ確保する枠）
- (3 件) 期間表記あり（5 種確保後の残り枠）


## ④ 表記ゆれ（6 件・FR-05）

全角％ / 半角% / カ月表記のゆれ。正規化後に同一 observation へ収束することを見る。

| 行 | 掲載日 | 記事タイトル | **決めること** |
|---:|---|---|---|
| 49 | 2026-04-06 | 農水省・食品等取引実態調査、家庭用値上げ不足24％ | **「既存店」表記なし** → 既存店指標に**昇格させない**。率なら `all-store-sales-yoy` / 絶対額なら `sales-amount-absolute`（カタログ §2.2）<br>**期間が読み取れない** → `ambiguous_period` で (B) にするかを含めて判断 |
| 50 | 2026-04-21 | 小売業の「支払手数料」負担、10年で45％増加 キャッシュレス決済普及で | **「既存店」表記なし** → 既存店指標に**昇格させない**。率なら `all-store-sales-yoy` / 絶対額なら `sales-amount-absolute`（カタログ §2.2）<br>**期間が読み取れない** → `ambiguous_period` で (B) にするかを含めて判断 |
| 51 | 2026-04-27 | 出前館、株価97％下落……なぜこうなった "海外勢"押し寄せたフードデリバリー、激戦の決着は | **「既存店」表記なし** → 既存店指標に**昇格させない**。率なら `all-store-sales-yoy` / 絶対額なら `sales-amount-absolute`（カタログ §2.2）<br>**期間が読み取れない** → `ambiguous_period` で (B) にするかを含めて判断 |
| 52 | 2026-04-06 | 食品物流の歪み顕在化、価格転嫁65％止まり | **「既存店」表記なし** → 既存店指標に**昇格させない**。率なら `all-store-sales-yoy` / 絶対額なら `sales-amount-absolute`（カタログ §2.2）<br>**期間が読み取れない** → `ambiguous_period` で (B) にするかを含めて判断 |
| 53 | 2026-05-22 | 世界の新車4台に1台がEV、日本は3％未満で停滞 | **「既存店」表記なし** → 既存店指標に**昇格させない**。率なら `all-store-sales-yoy` / 絶対額なら `sales-amount-absolute`（カタログ §2.2）<br>**期間が読み取れない** → `ambiguous_period` で (B) にするかを含めて判断 |
| 54 | 2026-06-13 | 運輸業96％に中東影響、名商調査 | **「既存店」表記なし** → 既存店指標に**昇格させない**。率なら `all-store-sales-yoy` / 絶対額なら `sales-amount-absolute`（カタログ §2.2）<br>**期間が読み取れない** → `ambiguous_period` で (B) にするかを含めて判断 |


## ⑤ 定性表現のみ・連続記録（6 件）

`増収増益` は **value=None / sign_only='+' / needs_source_check=True** の 2 レコード。`51カ月ぶりに前年割れ` は **value と streak_broken_months=51 の両方**を持つ（制約 11）。

| 行 | 掲載日 | 記事タイトル | **決めること** |
|---:|---|---|---|
| 11 | 2026-07-22 | スーパーマーケット／6月の既存店売上0.3％減、40カ月ぶりに前年割れ | `scope` = `existing_store`（「既存店」表記あり）でよいか<br>`period_key`: タイトルに年が無ければ**掲載月の前月**（カタログ §4.2）<br>`streak_broken_months` も入れる（**値と両方**。制約 11） |
| 12 | 2026-07-25 (+1) | ショッピングセンター／6月既存店売上1.6％減、夏物振わず51カ月ぶりに前年割れ | `scope` = `existing_store`（「既存店」表記あり）でよいか<br>`period_key`: タイトルに年が無ければ**掲載月の前月**（カタログ §4.2）<br>`streak_broken_months` も入れる（**値と両方**。制約 11） |
| 29 | 2026-03-31 (+1) | しまむら 決算／2月期増収増益、売上高・営業利益・純利益で過去最高を更新 | **「既存店」表記なし** → 既存店指標に**昇格させない**。率なら `all-store-sales-yoy` / 絶対額なら `sales-amount-absolute`（カタログ §2.2）<br>`period_key` / `period_type`（fiscal_period）<br>`value = null` / `sign_only` / `needs_source_check = true` |
| 30 | 2026-04-12 | 丸八倉庫 1Q、増収増益も修繕費響き物流減益 | **「既存店」表記なし** → 既存店指標に**昇格させない**。率なら `all-store-sales-yoy` / 絶対額なら `sales-amount-absolute`（カタログ §2.2）<br>`period_key` / `period_type`（quarter）<br>`value = null` / `sign_only` / `needs_source_check = true` |
| 31 | 2026-04-03 (+1) | クスリのアオキHD 決算／6〜2月増収増益、フード＆ドラッグ加速 | **「既存店」表記なし** → 既存店指標に**昇格させない**。率なら `all-store-sales-yoy` / 絶対額なら `sales-amount-absolute`（カタログ §2.2）<br>**期間が読み取れない** → `ambiguous_period` で (B) にするかを含めて判断<br>`value = null` / `sign_only` / `needs_source_check = true` |
| 32 | 2026-04-06 (+1) | オークワ 決算／2月期増収増益、中計の目標数値取り下げ | **「既存店」表記なし** → 既存店指標に**昇格させない**。率なら `all-store-sales-yoy` / 絶対額なら `sales-amount-absolute`（カタログ §2.2）<br>`period_key` / `period_type`（fiscal_period）<br>`value = null` / `sign_only` / `needs_source_check = true` |

選定根拠の内訳:

- (4 件) 定性表現のみ（期待値は value=None / sign_only / needs_source_check=True）
- (2 件) 連続記録表現 + 値あり + 対象内（期待値は value と streak_broken_months の**両方**）


## ⑥ 発表主体が並立する行（4 件）★末尾 3 区分

**期待値は「2 レコードが共存し、どちらも上書きされない」**（制約 14）。natural key の第 5 要素 `source_authority` だけが異なる。

| 行 | 掲載日 | 記事タイトル | **決めること** |
|---:|---|---|---|
| 3 | 2026-05-08 | 百貨店／3月の販売額2.2％増の5547億円、既存店は3.4％増（経産省調べ） | **何レコードに分解するか**（% トークン 2 個。FR-11）<br>`scope` = `existing_store`（「既存店」表記あり）でよいか<br>`source_authority`（経産省側 = `meti` / 協会側 = カタログ既定）。**相方と natural key が衝突せず 2 レコード共存すること**<br>`period_key`: タイトルに年が無ければ**掲載月の前月**（カタログ §4.2） |
| 4 | 2026-04-28 | 日本百貨店協会／3月の売上高3.2％増、国内顧客・インバウンドともにプラスに | **「既存店」表記なし** → 既存店指標に**昇格させない**。率なら `all-store-sales-yoy` / 絶対額なら `sales-amount-absolute`（カタログ §2.2）<br>`source_authority`（経産省側 = `meti` / 協会側 = カタログ既定）。**相方と natural key が衝突せず 2 レコード共存すること**<br>`period_key`: タイトルに年が無ければ**掲載月の前月**（カタログ §4.2） |
| 5 | 2026-06-30 | 百貨店／5月の販売額7.6％増の5137億円、既存店は8.8％増（経産省調べ） | **何レコードに分解するか**（% トークン 2 個。FR-11）<br>`scope` = `existing_store`（「既存店」表記あり）でよいか<br>`source_authority`（経産省側 = `meti` / 協会側 = カタログ既定）。**相方と natural key が衝突せず 2 レコード共存すること**<br>`period_key`: タイトルに年が無ければ**掲載月の前月**（カタログ §4.2） |
| 6 | 2026-06-26 | 日本百貨店協会／5月の売上高8.3％増、インバウンド16.7％増・国内顧客7.4％増 | **何レコードに分解するか**（% トークン 3 個。FR-11）<br>**「既存店」表記なし** → 既存店指標に**昇格させない**。率なら `all-store-sales-yoy` / 絶対額なら `sales-amount-absolute`（カタログ §2.2）<br>`source_authority`（経産省側 = `meti` / 協会側 = カタログ既定）。**相方と natural key が衝突せず 2 レコード共存すること**<br>`period_key`: タイトルに年が無ければ**掲載月の前月**（カタログ §4.2） |

選定根拠の内訳:

- (1 件) 発表主体の並立ペア（segment=department-store / 3月）の協会側。**相方と 2 レコードが共存するのが期待値**（制約 14）
- (1 件) 発表主体の並立ペア（segment=department-store / 3月）の経産省側。**相方と 2 レコードが共存するのが期待値**（制約 14）
- (1 件) 発表主体の並立ペア（segment=department-store / 5月）の協会側。**相方と 2 レコードが共存するのが期待値**（制約 14）
- (1 件) 発表主体の並立ペア（segment=department-store / 5月）の経産省側。**相方と 2 レコードが共存するのが期待値**（制約 14）


## ⑦ 数値が取れないことが正解の行（4 件）★末尾 3 区分

**期待値は `unresolved`（`reason_code = no_numeric`）**（制約 3）。「取れないことが正解」を評価データに含めないと、無理に数値をひねり出す方向へ最適化が進む。選定側では**ランキング記事を除外**し、「対象内の月次統計だが値が取れない」行に絞った（未決事項 (c) が決着していないため、それを前提にしない）。G1 が名指しする代表例 `ホームセンター月次実績＝2026年6月度` を含む。

| 行 | 掲載日 | 記事タイトル | **決めること** |
|---:|---|---|---|
| 7 | 2026-04-07 | 3月の百貨店売上高、全社増収＝免税売り上げは5カ月ぶりプラス | **(B)** `no_numeric` でよいか（業態は解決できるが値が無い） |
| 8 | 2026-03-31 (+1) | 令和8年2月度チェーンストア販売統計 | **(B)** `no_numeric` でよいか（業態は解決できるが値が無い） |
| 9 | 2026-04-01 | コンビニエンスストア統計調査2月度 | **(B)** `no_numeric` でよいか（業態は解決できるが値が無い） |
| 10 | 2026-04-23 | ホームセンター月次実績＝2026年3月度 | **(B)** `no_numeric` でよいか（業態は解決できるが値が無い） |


## ⑧ 対象範囲外が正解の行（6 件）★末尾 3 区分

**期待値は `unresolved`（`reason_code = out_of_scope`）**（制約 15）。ただし**うち 2 件は真の取りこぼしで期待値は `no_segment_match`**。この 2 種の判別が評価できないと、NFR-05 の分母操作を誰も検出できなくなる。

| 行 | 掲載日 | 記事タイトル | **決めること** |
|---:|---|---|---|
| 1 | 2026-05-05 | 4月都内物価、1.5%上昇＝5カ月連続伸び縮小―総務省 | **(B)** `no_segment_match`（取りこぼし＝カタログに業態を足すべき）か `out_of_scope`（対象外）か ← **G-4 の判断が直接効く** |
| 2 | 2026-06-27 | 6月都内物価、1.6％上昇＝8カ月ぶり伸び拡大―総務省 | **(B)** `no_segment_match`（取りこぼし＝カタログに業態を足すべき）か `out_of_scope`（対象外）か ← **G-4 の判断が直接効く** |
| 55 | 2026-04-02 (+1) | NRF is forecasting U.S. retail sales to grow 4.4% in 2026 | **(B)** `out_of_scope` でよいか（個社決算 / 非統計記事） |
| 56 | 2026-04-20 | ニトリHDの株価が36営業日ぶりに更新、その背景にある複雑な事情 | **(B)** `out_of_scope` でよいか（個社決算 / 非統計記事） |
| 57 | 2026-04-17 (+1) | 首都圏物流施設の供給量が縮小、2027年に過去最低水準へ | **(B)** `out_of_scope` でよいか（個社決算 / 非統計記事） |
| 58 | 2026-04-02 (+1) | NRF forecasts 4.4% retail sales growth this year despite rising uncertainty | **(B)** `out_of_scope` でよいか（個社決算 / 非統計記事） |

選定根拠の内訳:

- (4 件) 業態別名にも発表主体マーカーにも一致しない（個社・非統計記事）
- (2 件) 発表主体マーカーはあるが業態を解決できない = **真の取りこぼし候補**（期待値は no_segment_match。out_of_scope との判別を評価するため意図的に混ぜる）


---

## 確定後の手順

```bash
# 1. 候補の expected を埋めて status を confirmed にする
#    （golden-60.candidates.jsonl を編集）
# 2. 凍結
cp scripts/retail-stats-tracker/tests/fixtures/golden-60.candidates.jsonl \
   scripts/retail-stats-tracker/tests/fixtures/golden-60.jsonl
# 3. 構成と確定状態を検査
cd scripts/retail-stats-tracker && python3 -m unittest tests.test_golden60
```

`test_golden60.TestGolden60Frozen` は、凍結ファイルの全 60 行が `status: confirmed` かつ `expected` が非 null であることを検査する。未確定の行が混ざっていれば落ちる。
