# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# 一時ファイル（scripts/retail-stats-tracker/tests/make_fixtures.py 等の
# 作業用出力。生成データそのもの（data/*.json）は将来コミット対象になるため
# ここでは除外しない）
/tmp/
*.tmp

# 検証 hooks（B 系統）の実行時状態。loop-engineering-design.md §2.2 の
# RS_STATE_DIR。Stop の再入ガードのセッション別カウンタと unresolved-stops.log
# であり、成果物ではない。commit すると session_id が履歴に残り続ける。
.companies/*/.retail-stats-verify/
