/*
 * ビュー切替・描画・CSV出力（実装設計 §6 / §8 M6）。
 *
 * TODO(M6): SC-01〜SC-06 の実装。
 *   - SC-01/02: 業態×指標のトレンドチャート（重ね合わせ UI は実装設計スコープ外、
 *     実装時に確定。R1/R6 の禁則をここでも二重に検査すること）
 *   - SC-03/04: テーブルビュー、CSV エクスポート
 *   - SC-05: 出典リンク（FR-17 全データ点から出典 URL に到達できること）
 *   - SC-06: データ品質パネル（P1/P2/P3 の3分割。P3「対象外」は
 *     out_of_scope_breakdown を独立表示し、未解決件数と混ぜない）
 *
 * 禁則（NFR-08 自己完結・NFR-13）:
 *   - fetch( / import( / src="http を一切使わないこと（外部参照禁止）
 *   - 色のみに依存した情報伝達をしないこと（NFR-13）
 */

(function () {
  "use strict";
  // TODO(M6): series-data の JSON を読み込み、チャート・テーブルを描画する。
})();
